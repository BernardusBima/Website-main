---
layout: "layout.njk"
title: "Artikel"
permalink: "/artikel/"
description: "Kumpulan artikel, wawasan, dan berita hukum terkini dari LBH Ratu Adil Semarang."
---

<div class="page-header">
    <div class="container">
        <h1>Artikel Hukum</h1>
        <p>Temukan wawasan, analisis mendalam, dan berita terbaru seputar dunia hukum yang disajikan secara ringkas dan mudah dipahami.</p>
    </div>
</div>

<section id="artikel-list">
    <div class="container">
        <div class="article-grid">
            {# Ini adalah loop untuk menampilkan semua artikel, yang terbaru di atas #}
            {%- for post in collections.artikel | reverse -%}
            <article class="article-card">
                <div class="article-card-image">
                    <a href="{{ post.url }}">
                        <img src="{{ post.data.thumbnail }}" alt="Gambar thumbnail untuk {{ post.data.title }}">
                    </a>
                </div>
                <div class="article-card-content">
                    <span class="article-category">{{ post.data.category }}</span>
                    <h3><a href="{{ post.url }}" style="text-decoration:none; color:inherit;">{{ post.data.title }}</a></h3>
                    <p>{{ post.data.summary }}</p>
                    <a href="{{ post.url }}" class="read-more">Baca Selengkapnya &rarr;</a>
                </div>
            </article>
            {%- else -%}
                <p>Belum ada artikel yang dipublikasikan.</p>
            {%- endfor -%}
        </div>
    </div>
</section>